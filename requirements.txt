# --- Web framework / server ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Config ---
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Google Gmail + OAuth ---
google-api-python-client==2.156.0
google-auth==2.37.0
google-auth-oauthlib==1.2.1

# --- Secure token storage (macOS Keychain) ---
keyring==25.6.0

# --- HTTP client (local LLM, OpenAI-compatible endpoint) ---
httpx==0.28.1

# --- Cloud LLM provider (only used when LLM_PROVIDER=anthropic) ---
anthropic==0.42.0

# --- Tests ---
pytest==8.3.4
